<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%=link_to "edit", edit_task_path(@task.id) %>

<%= link_to "Delete task",
            task_path(@task.id),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
